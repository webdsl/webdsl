Checkout this directory as an Eclipse project, then use the 'Convert to a WebDSL Project' wizard in the right-click menu of the project.

The tutorial-base-v*/ directory is everything except v*.app which contains the completed assignments.