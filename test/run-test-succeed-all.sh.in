#!/bin/bash

SRCDIR=$(pwd)/../src
cd `dirname $0`
FILE=`basename $0 .sh`
bash @prefix@/bin/webdsl test $FILE &> $FILE.out

result=$?
if test 0 -ne $result; then
  cat $FILE.out
fi
rm -f $FILE.out
exit $result
