module entitytest

entity Foo {
  name : String (name)   
  name : Int (id, name)  
  bar : Set<Bar>
}

entity Bar {
  bla : Foo (inverse:bar)  
}

define page bar() {
  output(h)
}

define page foo(f : Foo) { 
  output(g)
  form { 
    input(g)  
    output(g.name)
    submit bla() { "Bla" }
  } 
}


