module entitytest

entity Foo {
  title : String (name)   
  title : String (id)  
  bar    : Set<Bar>
}

entity Bar {
  bla : Foo (inverse:bar)   
}

define page bar() {
  output(h) 
}

define output(f : String) { }
 
define page foo(f : Foo) { 
  ouput(g)
  form {
    output(g.name)
    output(f.title)
    submit bla() { "Bla" }
  } 
  
  action bla4() {
  }
}


