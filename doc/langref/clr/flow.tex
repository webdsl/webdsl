\chapter{Control flow elements}

	%%TODO kunnen eze items rechtstreeks aangesproken worden?
%	\item[Select(x, type-to-select-from, label, current-selection)]\index{select} result in a select box being createad. It is able to select a number of objects from all objects in the collection\emph{type} available. So the whole collection of a certain type will be displayed. If the provided object is a collection (\emph{List} or \emph{Set}) multiple items can be selected, otherwise just a single item can be selected. %TODO: x??
%	\item[SelectFromList(current-selection, available-items)]\index{selectFromList}\index{dropdown} creates a dropdown box from which items can be selected. 
	
