module modules

rules

  declare-all =
    declare-primitive-types;
    desugar-all;
    alltd(declare-def); 
    rename-all

  rename-all =
    alltd(rename)
    
  desugar-all =
    innermost(desugar)
  
  module-name :
    Module(x, d*) -> x
    