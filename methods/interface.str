module separation/methods/interface

strategies
  
  get-recorded-rules = fail
  
  get-cached-rules = fail
  
  load-dependent-defs-for-rules(|def) = fail
  
  before-rule-use(|name, key) = fail
  
  mark-rule-use(|name, key) = fail