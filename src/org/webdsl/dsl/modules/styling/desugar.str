
module org/webdsl/dsl/modules/styling/desugar

imports
  libstrategolib

imports
  libwebdsl-front

rules

  desugar :
    StylePropertyValue(MatchDefinition(name, farg*), prop) -> <StyleValue> (name, <map(?Arg(_,<id>))> farg*, prop)

  desugar :
    Var(x) -> <StyleVariable> x
