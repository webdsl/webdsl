<project name="webdsl-build-index" default="reindex">
  
  
  <target name="reindex">
    <echo>Now going to reindex (be sure to remove the existing indexes first)</echo>
    <ant dir="${currentdir}/.servletapp" antfile="${currentdir}/.servletapp/build.xml" target="reindex"/>	
  </target>
  
  
</project>
